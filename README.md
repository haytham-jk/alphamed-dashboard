

### Customer contacts and shared case badges

Customer cards support multiple laboratory contacts with name, designation, number, and email. Customer and contact writes use the atomic `save_customer_with_contacts` function. Case status and priority badge colors are shared through `src/constants/caseDisplay.js`. The New Case form scrolls and focuses the first invalid required field. Sidebar navigation remains Dashboard-first and alphabetic afterward.
